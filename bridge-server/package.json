{
  "name": "bridgetab-server",
  "version": "0.1.0",
  "description": "Localhost bridge server for the BridgeTab Chrome extension",
  "main": "src/cli.js",
  "bin": {
    "bridgetab": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "check": "node --check src/*.js && node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
